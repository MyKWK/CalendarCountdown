# 知行 1.0.15

## 展示层调整

- 任务列表与使命关联任务列表不再显示工作量数字和“点”计量单位。
- 新建任务、编辑任务仍保留工作量选择，用于任务评估和内部加权计算。
- 使命行双击展开/收起加入轻微弹性动画；关联任务卡片双击直接打开编辑。
- 使命行与任务卡片采用分离的双击命中区域。
- 不修改任务数据模型、使命进度、同步或 CLI 语义。

## 版本与边界

- 版本 `1.0.15`，构建号 `16`。
- 发行包为本机 ad-hoc 签名，不等同于 Developer ID 签名、Apple 公证、真机或 CloudKit 跨设备验收。
